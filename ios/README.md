# TrueCaddie iOS

The iOS runtime consumes published course bundles and eventually runs the on-device strategy engine and voice flow.

The first foundation slice adds a small domain module contract for loading and inspecting a canonical bundle. Swift tooling is not available in this local Windows environment, so these files are intended as the initial source layout and contract until they are opened in an Apple toolchain.
