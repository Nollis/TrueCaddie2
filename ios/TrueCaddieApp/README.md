# TrueCaddie App Scaffold

This directory is the future iOS app shell.

The first foundation slice only adds a lightweight bundle inspector view scaffold. It is intentionally not wired into an Xcode project yet; the shared domain package owns the bundle decoding contract.
