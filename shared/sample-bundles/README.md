# Sample Bundles

Published sample bundles live here so Course Studio and the iOS runtime can work against the same fixture.

The first generated bundle is expected at:

- `kungsbacka-nya.v1.json`

Regenerate it with:

```powershell
node course-studio/app/publish-kungsbacka-nya.mjs
```
